import React, { useContext } from "react";
import ImageMenu from "./ImageMenu";

const ProjectMenu = () => {
    return (
        <div>
            <ImageMenu />
        </div>
    );
};

export default ProjectMenu;